This folder contains the backend and code used to populate the backend database.

create_data_script.js runs in the browser and creates the database json file, and
after running that, you can run translate_names.js to add translated text and
create an updated .json file that the backend uses.
